[pytest]
testpaths = tests
asyncio_mode = auto
# Pin the fixture loop scope explicitly rather than inheriting a default
# that pytest-asyncio warns about and plans to change.
asyncio_default_fixture_loop_scope = function
filterwarnings =
    error::DeprecationWarning:app.*
